<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="ir_cron_academy_expire_reservations" model="ir.cron">
        <field name="name">Academy: Expire Enrollment Reservations</field>
        <field name="model_id" ref="academy_lab.model_academy_enrollment"/>
        <field name="state">code</field>
        <field name="code">model._cron_expire_reservations()</field>
        <field name="active" eval="True"/>

        <field name="interval_number">1</field>
        <field name="interval_type">hours</field>
    </record>

</odoo>
